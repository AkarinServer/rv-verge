# 构建状态监控指南

## 自动监控脚本

项目包含一个自动监控脚本 `monitor_builds.sh`，可以每1分钟自动检查一次构建状态。

### 使用方法

```bash
cd /Users/lolotachibana/dev/tauri-test
./monitor_builds.sh
```

脚本会：
- ✅ 每60秒（1分钟）自动刷新状态
- 📊 显示最新的3个构建状态
- 🔄 实时更新状态图标（✅成功、❌失败、🔄进行中、⏳排队）
- ⏱️ 显示下次检查时间

按 `Ctrl+C` 停止监控。

### 功能特点

- **自动刷新**: 每1分钟自动检查一次
- **清晰显示**: 使用图标和颜色标识状态
- **多构建监控**: 同时显示多个 workflows 的状态
- **时间显示**: 显示构建时间和下次检查时间

## 其他监控方式

### 1. GitHub CLI Watch 命令

```bash
# 实时监控最新构建
gh run watch --repo AkarinServer/tauri-test

# 监控特定构建
gh run watch <run-id> --repo AkarinServer/tauri-test
```

### 2. 一次性检查

```bash
# 查看最新构建列表
gh run list --repo AkarinServer/tauri-test --limit 5

# 查看特定构建详情
gh run view <run-id> --repo AkarinServer/tauri-test

# 查看构建日志
gh run view <run-id> --repo AkarinServer/tauri-test --log
```

### 3. 在浏览器中查看

访问: https://github.com/AkarinServer/tauri-test/actions

GitHub Actions 页面会自动刷新，可以看到实时状态。

### 4. GitHub 通知

在 GitHub 设置中启用通知，构建完成时会收到邮件或推送通知。

## 状态说明

- ✅ **成功** (success): 构建成功完成
- ❌ **失败** (failure): 构建失败
- 🔄 **进行中** (in_progress): 正在构建
- ⏳ **排队** (queued): 等待开始构建
- ⚠️ **取消** (cancelled): 构建被取消

## 示例输出

```
═══════════════════════════════════════════════════════════
  GitHub Actions 构建状态监控 - 2025-11-11 12:15:30
═══════════════════════════════════════════════════════════

最新构建状态:

🔄 Build RISCV64 (Simple)    in_progress   N/A        12:13:52
🔄 Build RISCV64 with Docker  in_progress   N/A        12:13:52
🔄 Build for RISCV64          in_progress   N/A        12:13:52

═══════════════════════════════════════════════════════════
下次检查: 12:16:30
按 Ctrl+C 停止
```

## 故障排除

### 脚本无法运行

```bash
# 确保脚本有执行权限
chmod +x monitor_builds.sh

# 确保 GitHub CLI 已安装并登录
gh auth status
```

### 无法连接到 GitHub

```bash
# 检查网络连接
ping github.com

# 重新登录 GitHub CLI
gh auth login
```

